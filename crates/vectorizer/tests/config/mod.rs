//! Integration tests for the layered config loader against the real
//! checked-in `config.example.yml` + `config/modes/*.yml` fixtures.

#![allow(clippy::unwrap_used, clippy::expect_used)]

pub mod layered_real_files;
