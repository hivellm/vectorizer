#!/bin/bash

# Vectorizer wrapper script
# This script provides a convenient way to run the vectorizer CLI

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the vzr binary with all passed arguments
exec cargo run --bin vzr -- "$@"
