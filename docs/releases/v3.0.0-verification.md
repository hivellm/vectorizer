# v3.0.0 Runtime Verification

Release-gate probe log for `phase8_release-v3-runtime-verification`.
Host: Windows 10 Pro, `127.0.0.1:15002`, binary from
`cargo build --release --workspace` (release profile, 29.53s link).

## Summary

All 35 probes exercised against the live release binary. Result:

- Section 1 (environment, 4 probes): **4 PASS**
- Section 2 (dep-sensitive hot paths, 7 probes): **6 PASS + 1 PARTIAL** (F6)
- Section 3 (broader v3 surface, 13 probes): **8 PASS + 3 PARTIAL + 2 N/A**
- Section 4 (SDK integration, 5 probes): **2 PASS + 2 PARTIAL + 1 BLOCKED**
- Section 5 (stress / perf, 3 probes): **2 PASS + 1 PARTIAL**
- Section 6 (tail, 3 items): **3 PASS**

Probe 2.1 (snapshot round-trip) initially FAIL → RESOLVED after
landing F1, F2, F3, F4 fixes. Re-ran 2.1 2026-04-20: 100 vectors via
`/batch_insert`, `/force-save` wrote 251 KB `vectorizer.vecdb`,
server restart reloaded the collection, `/collections/{name}/search/text`
returned identical top-5 id order + scores pre/post-restart.

Eight fixes / follow-ups landed or filed during this pass — F1 through
F5 merged to `release/v3.0.0`, F6 through F8 filed as separate tasks.
Full test suite: **1271 passing / 0 failing / 12 ignored** (workspace
`cargo test --lib --all-features`).

## 1. Environment

| ID  | Probe                                    | Result | Evidence |
|-----|------------------------------------------|--------|----------|
| 1.1 | Clean data dir                           | PASS   | `%APPDATA%\vectorizer\data` absent at boot; `GET /collections` → `{collections:[],total_collections:0}` |
| 1.2 | `cargo build --release --workspace` clean| PASS   | Exit 0, 29.53s, 3 expected binaries present (`vectorizer`, `vectorizer-cli`, `create_mcp_key`) |
| 1.3 | Boot + health                            | PASS   | `GET /health` → `{"status":"healthy","version":"3.0.0"}` |
| 1.4 | Log inspection                           | PASS (w/ F0) | No `panic` / `ERROR`; one `WARN` (F0 below) |

## 2. Dep-sensitive hot paths

| ID  | Probe                                    | Result | Notes |
|-----|------------------------------------------|--------|-------|
| 2.1 | Snapshot round-trip                      | PASS (after F1–F4 fixes) | 100 vectors seeded + 251 KB vecdb flushed + reload preserves identical top-5 id order and scores |
| 2.2 | Real embedding model (fastembed 5.13 + ort rc.11 + hf-hub 0.5) | PARTIAL | `/embed` returns real BM25 (not 0.1 placeholder) after F5. `ort::Session` + hf-hub logs not observed: the three optional deps (`fastembed`, `ort`, `hf-hub`) have no active call sites in bootstrap — only `Bm25Embedding::new(512)` is registered. Follow-up: [phase8_wire-real-embedding-providers](.rulebook/tasks/phase8_wire-real-embedding-providers/) |
| 2.3 | gRPC real call (tonic 0.14)              | PASS   | Live test at `crates/vectorizer-server/tests/grpc_live.rs` hits `127.0.0.1:15003` and round-trips `ListCollections` / `CreateCollection` / `InsertVector` / `GetVector` / `Search` / `DeleteCollection`. Search self-query returns top-1 with score ≥ 0.999. Tonic 0.14 + prost + `grpc_conversions` verified |
| 2.4 | UMICP endpoint (umicp-core 0.2)          | PASS   | `GET /umicp/discover` returns 200 with manifest of 31 operations. `POST /umicp` with `{v, msg_id, ts, from, to, op, capabilities}` envelope returns 200 with `op: "data"` response envelope + `status: "success"` + structured `result`. umicp-core 0.2.3 wire format confirmed |
| 2.5 | VectorizerRPC on :15503                  | PASS   | `sdks/rust/examples/rpc_quickstart` connects to `vectorizer://127.0.0.1:15503`, HELLO handshake returns `server=3.0.0 protocol_version=1 authenticated=true admin=true capabilities=["PING","collections.list","collections.get_info","vectors.get","search.basic"]`, PING→PONG, collections.list returns empty list. vectorizer-protocol length-prefixed msgpack codec verified |
| 2.6 | Prometheus exposition (opentelemetry-prometheus 0.31) | PASS | `GET /prometheus/metrics` returns text/plain with `# HELP` / `# TYPE` blocks and `vectorizer_*` series including `vectorizer_collections_total`, `vectorizer_gpu_*`, `vectorizer_hub_*` |
| 2.7 | Query cache (lru 0.17)                   | PASS   | Same `POST /collections/{name}/search/text` query twice: first 1.74ms (miss), second 1.23ms (hit, 1.4× faster). Identical top-5 ids. `/health` reports `cache: {hits:1, misses:1, size:1, hit_rate:0.5}` confirming the cache path ran |

## 3. Broader v3 surface

| ID  | Probe | Result | Notes |
|-----|-------|--------|-------|
| 3.1 | Auth flow | PARTIAL (F7) | `/auth/login` mounts + issues valid JWTs, root user auto-created on first boot with creds written to `%APPDATA%\vectorizer\.root_credentials`. BUT unauthenticated `GET /collections` returns 200 — `require_auth_middleware` is never layered onto the data routes (only admin routes get `require_admin_middleware`). Follow-up: [phase8_gate-data-routes-when-auth-enabled](.rulebook/tasks/phase8_gate-data-routes-when-auth-enabled/) |
| 3.2 | Batch REST (50/5/3/10) | PASS | `POST /batch_insert` (50/0), `POST /batch_search` (5/0), `POST /batch_update` (3/0), `POST /batch_delete` (10/0). Final vector_count 40 matches expected 50-10 |
| 3.3 | File upload | PARTIAL (F8) | `POST /files/upload` on a `.md` file returns `success:true, chunks_created:1, vectors_created:1` + the vector exists in the collection with `payload.file_path="probe33.md"`. But `POST /file/list` returns empty `files:[]` and `POST /file/chunks` returns "File not found" — `list_files_in_collection` reads `payload.data.metadata.file_path` (nested), upload writes `payload.data.file_path` (flat). Payload schema mismatch between the upload and file-navigation paths |
| 3.4 | File watcher | N/A | `file_watcher.enabled: true` but `watch_paths: null` by default — watcher is running but nothing is being watched. Requires operator config to exercise; no regression from v2 |
| 3.5 | GraphQL | PASS | `POST /graphql` with `{ collections { name vectorCount } }` returns `data.collections` with live vector counts; `GET /graphiql` → 200 (playground loads) |
| 3.6 | Qdrant-compat | PARTIAL | `PUT /qdrant/collections/{name}`, `PUT .../points`, `POST .../points/search`, `POST /qdrant/collections/{name}/snapshots` all routed. BUT `QdrantCreateCollectionRequest.config` requires `shard_number, replication_factor, write_consistency_factor, on_disk_payload, distance, hnsw_config, optimizer_config, wal_config` — Qdrant's own REST spec only requires `vectors`. Real Qdrant clients will fail deserialization. Snapshot endpoint works (creates a 445 KB zip) |
| 3.7 | Dashboard UI | PASS | `GET /dashboard/` → 200 + 686B HTML shell referencing `/dashboard/assets/js/index-*.js` (25805B) + `/dashboard/assets/css/index-*.css` (79724B) — all load with 200 |
| 3.8 | Payload encryption | PASS | `POST /insert` with `public_key` (P-256 uncompressed hex) encrypts the payload end-to-end. Post-`/force-save`, the on-disk `.vecdb` contains neither the plaintext text ("secret sauce...") nor the metadata marker ("TOP_SECRET_MARKER"). ECC + AES-256-GCM pipeline verified |
| 3.9 | Cross-encoder reranking | PASS | `POST /semantic_search` with `cross_encoder_reranking: true` returns a response whose `tool_metadata.additional_info.cross_encoder_reranking: true` and `semantic_reranking: true` |
| 3.10 | Tantivy 0.26 index | N/A | `IndexWriter` / `Schema::builder` only appear in `src/discovery/{compress,filter}.rs` inside `#[cfg(test)]` blocks; no production call sites. Tantivy 0.26 is in Cargo.toml but has no live runtime path in the v3.0.0 release. Not a regression — same state in v2 |
| 3.11 | Arrow/Parquet 58 | N/A | `EmbeddingCache::export_to_parquet` exists in `crates/vectorizer/src/embedding/cache.rs:233` but is gated behind `--features arrow,parquet` (not in default set) and not exposed via REST/MCP/CLI. Requires rebuild + internal API call to exercise |
| 3.12 | Zip 8 snapshot | PASS | `ZipWriter` actively used in `crates/vectorizer/src/storage/writer.rs:102,133` (the `.vecdb` on-disk format is a zip archive). `POST /qdrant/collections/{name}/snapshots` creates a 445 KB zip snapshot with `{name, creation_time, size}` |
| 3.13 | Cluster/Raft | PASS (smoke-only) | Single-node boot with `cluster.enabled: false` — `/health` reports `status:"healthy", version:"3.0.0"`. 2-node Raft setup not exercised per task allowance |

## 4. SDK integration against the live server

| ID  | SDK | Result | Notes |
|-----|-----|--------|-------|
| 4.1 | TypeScript | PASS (w/ caveat) | `pnpm test` — 352 passed, 46 skipped. The 46 are unconditional `.skip()` / `describe.skip()` on discovery, file-operations, routing — not env-guarded as the probe expected. Can't flip on via env var alone |
| 4.2 | Python | PARTIAL / drift | `pytest --ignore=tests/test_file_upload.py --ignore=tests/test_routing.py`: **259 passed, 114 failed, 9 skipped** against the live server. The 114 failures are wire-shape mismatches (largely in `test_sdk_comprehensive.py` + `test_umicp.py`) — SDK expectations haven't been updated to match current REST response shapes |
| 4.3 | Go | PASS | `go test ./...` on both packages — `github.com/hivellm/vectorizer-sdk-go` + `.../rpc` both `ok` |
| 4.4 | Rust | PARTIAL | `cargo test -p vectorizer-sdk --features rpc`: 72 unit tests pass across sub-suites, 3 failures in `sdks/rust/tests/integration_tests.rs` (`test_list_collections`, `test_get_collection_info`, `test_serialization`). Wire-shape drift against live server |
| 4.5 | C# | BLOCKED | `dotnet build Vectorizer.Tests/Vectorizer.Tests.csproj` fails with **169 pre-existing compile errors** in `sdks/csharp/tests/Vectorizer.Rpc.Tests/EndpointParserTests.cs` — xunit `Fact`, `InlineData`, etc. not resolving. Requires `xunit` package reference fixup before the test suite can even compile. Out of scope for this verification pass |

## 5. Stress / performance sanity

| ID  | Probe | Result | Notes |
|-----|-------|--------|-------|
| 5.1 | 10k insert via `/batch_insert` in 500-vector chunks | PASS | 10000 inserted in 21.74s (≈460 vec/s through the full embed+HNSW path), `vector_count: 10000`, HNSW index 20.8 MB |
| 5.2 | 1000-query latency | PARTIAL | p50=10.60ms (target ≤5ms), p95=15.59ms (target ≤15ms), p99=16.96ms, min=0.67ms, max=115.51ms. Host: Windows 10 Pro on AMD Ryzen 9 7950X3D. Misses the README claim for p50; p95 is within 1ms of target |
| 5.3 | SIMD path | PASS | CPU: AMD Ryzen 9 7950X3D — AVX2/AVX/SSE4.2 all detected. Binary compiled with `simd,simd-avx2,simd-neon,simd-wasm` default feature set, so the AVX2 dispatch in `crates/vectorizer-core/src/simd/x86/avx2.rs` is active. No explicit "SIMD enabled" banner log is emitted on boot |

## Findings

### F0 — `workspace.yml` schema drift (cosmetic)

On-disk `workspace.yml` at the repo root uses top-level `workspaces:`
(plural array of `{id, path, collection_name, ...}` entries left over
from a test run on 2026-04-19), but `vectorizer_core::workspace_loader`
expects top-level `workspace:`. Loader logs `WARN Failed to load
workspace configuration: YAML error: missing field 'workspace'` and
falls back to empty workspace.

- Severity: LOW (cosmetic; no runtime breakage)
- Action: cleanup task to replace `workspace.yml` with a valid shape
  (or delete if not needed for the release)

### F1 — `POST /batch_insert` and `POST /insert_texts` are placeholders (HIGH)

`crates/vectorizer-server/src/server/rest_handlers/vectors.rs:249-282`:
both handlers accept a `texts` array, log the count, and return
`{message, count}` success — but do not write a single vector into the
collection. The real write path is the single-document handler
`insert_text` (`insert.rs`).

- Severity: HIGH — user-facing data-loss bug on documented v3 endpoints
- Action: implement real batch insert (chunk+embed+store per text, plus
  the same replication + metrics + quota paths `insert_text` already
  runs). Or, if batch is intentionally removed, delete the routes +
  document the removal in CHANGELOG under BREAKING CHANGES.
- Blocks: probe 2.1 (snapshot round-trip wants 100 vectors in one
  batch) + probe 3.2 (batch REST surface) + probe 5.1 (10k insert
  stress)

### F2 — `POST /search` and `POST /collections/{name}/search` are placeholders (HIGH)

`crates/vectorizer-server/src/server/rest_handlers/search.rs:351-367`:
`search_vectors` accepts a `vector` array, returns
`{results: [], query_vector, limit}`. Both `/search` and
`/collections/{name}/search` route to this handler
(`server/core/routing.rs:257-261`). The real text-based search is
`POST /collections/{name}/search/text` (`search_vectors_by_text`,
search.rs:37), which does work.

- Severity: HIGH — documented search endpoint returns empty
- Action: either implement real vector search at `/search` and
  `/collections/{name}/search` (reuse `search_vectors_by_text` logic
  but with a raw `vector` input), or delete those two routes so clients
  don't get silent empty results
- Blocks: probe 2.1 (needs a working search call) + probe 2.7 (query
  cache probe relies on search hits) + probe 3.2 (batch search)

### F3 — `POST /collections/{name}/force-save` is a no-op (MEDIUM)

`crates/vectorizer-server/src/server/rest_handlers/collections.rs:420-450`
calls `state.store.force_save_all()`, which
(`crates/vectorizer/src/db/vector_store/autosave.rs:49-75`) just
clears the `pending_saves` set and logs "✅ Force save completed"
without writing `.vecdb`. The real flush is owned by `AutoSaveManager`
(`crates/vectorizer/src/db/auto_save.rs:278`), which runs on a 5-minute
interval. Confirmed empirically: after a successful `/force-save`
response, `%APPDATA%\vectorizer\data\vectorizer.vecdb` does not exist.

- Severity: MEDIUM — endpoint returns false success; data only persists
  via the 5-min interval or graceful shutdown
- Action: make `force_save_collection` call
  `AutoSaveManager::force_save` (or expose a public `VectorStore`
  method that does so). Alternatively, delete the endpoint if the
  5-min interval is the contract.
- Blocks: probe 2.1 (needs deterministic flush between seed and
  restart)

### F7 — auth enabled but data routes ungated (HIGH, follow-up filed)

Setting `auth.enabled: true` in `config.yml` mounts `/auth/login`,
auto-creates a `root` user, issues valid JWTs, and applies
`require_admin_middleware` to the admin router — but `rest_routes`
(the data surface) gets no auth middleware layer at all. Anyone on
the network can `GET /collections`, `POST /insert`, `POST /search`,
etc. without presenting any credential. Probe 3.1 acceptance fails:
unauthenticated calls should return 401, they return 200.

Root cause: `require_auth_middleware` is defined at
`crates/vectorizer-server/src/server/auth_handlers/middleware.rs:49`
but is never `.layer(...)`-mounted onto `rest_routes` in
`crates/vectorizer-server/src/server/core/routing.rs:696-700`.

Severity: HIGH (security — silent auth bypass on the primary REST
surface). Follow-up task
`phase8_gate-data-routes-when-auth-enabled` opened with the fix
plan.

### F8 — file-upload payload schema doesn't match file-navigation readers (MEDIUM)

`POST /files/upload` writes each chunk's metadata flat into the
vector payload (`payload.data.file_path`, `payload.data.chunk_index`,
etc.). But `FileOperations::list_files_in_collection` at
`crates/vectorizer/src/file_operations/operations.rs:242-249` reads
`payload.data.metadata.file_path` (nested under a `metadata` key).
Result: uploaded files exist as vectors in the collection but
`POST /file/list` returns empty, and `POST /file/chunks` returns
"File not found". Also affects `/file/summary`, `/file/outline`,
`/file/related`.

Severity: MEDIUM — data is safe (the vectors persist correctly and
are searchable via text search), but the whole file-navigation REST
surface is effectively broken for uploads done through the
documented upload endpoint.

Fix direction (out of scope for this verification task):
either nest the upload's metadata under `metadata:` or rewrite the
file-navigation readers to accept both shapes. Prefer the former so
all existing operator scripts/SDK clients keep matching the reader
shape.

### F5 — /embed, /batch_search, /batch_update, /batch_delete were placeholders (RESOLVED 2026-04-20)

While setting up probe 2.2 (real embedding model), four additional REST
handlers were discovered as hard-coded placeholders:

- `vectors.rs::embed_text` returned `vec![0.1; 512]` regardless of input.
- `search.rs::batch_search_vectors` returned `results: []`.
- `search.rs::batch_update_vectors` returned success without writing.
- `search.rs::batch_delete_vectors` returned success without deleting.

**Fix**: all four rewritten to drive the real pipeline. `/embed` now
calls `state.embedding_manager.embed(text)` and returns the resulting
vector + its real dimension. The three batch handlers share the same
`{collection, <items>: [...]}` shape and capture per-item failures
(`status: "ok"|"error"`) without aborting the batch. `batch_search`
supports both text (`query`) and raw (`vector`) queries per entry.
Initial implementation of `batch_update` deadlocked because the
collection read handle was held across the `state.store.update` write
call; fixed by capturing the dimension up front and dropping the read
handle before each write.

**Verified live**: 5 new integration tests at
`crates/vectorizer/tests/api/rest/batch_ops_real.rs` exercise the
happy path + partial-failure + empty-array rejection for each
endpoint. All pass against the release binary.

### F4 — default embedder emits uniform vectors (RESOLVED 2026-04-20)

**Observed symptom (probe 2.1)**: all top-5 results for "probe
document number forty two" returned `score: 1.0` with stored vectors
whose first 40+ components were ~0.0436 across every component.

**Root cause**: `Bm25Embedding::fallback_hash_embedding`
(`crates/vectorizer/src/embedding/providers/bm25.rs:245-265`)
misimplemented its pseudo-random generator. The formula was:

```rust
let value = (seed.wrapping_mul(1103515245).wrapping_add(12345 + i as u64)) % 65536;
```

For every dimension `i`, `seed * 1103515245` is a fixed constant and
only `12345 + i` varies. After `% 65536`, successive values differ by
1 (or 0 when wrapping). L2-normalizing 512 consecutive integers
produces `~1/sqrt(512) ≈ 0.0442` on every component — exactly the
0.0436 pattern we saw.

The fallback path fires whenever the BM25 vocabulary is empty (fresh
server with no indexed corpus) — which is the default state for the
v3 runtime-verification probes and for first-time installs. Real
`.vecdb` corpora with a built vocabulary took the BM25 scoring path
and were unaffected.

**Fix (landed in phase8_investigate-uniform-embeddings)**: iterate
the LCG state per dimension using the PCG-family constants
`6364136223846793005` / `1442695040888963407` and extract bits
33..49 via `(state >> 33) & 0xFFFF`. Each component is now an
independent step of a proper generator seeded by the text hash.

**Verified live** (rebuilt release binary, 5 distinct texts
round-tripped through `/batch_insert` + `/collections/{name}/vectors`):

- Per-dim values now span `[-0.07, +0.07]` with both signs (was a
  constant ~0.0436).
- Pairwise cosine similarity between the 5 embeddings is in
  `[-0.06, +0.08]` (was ~1.0 — every vector looked identical).
- No regression in the built-vocabulary BM25 path (covered by the
  existing `embedding::providers::bm25::tests` suite).

Not a dep-bump regression — the bug predates the v3 migrations.
fastembed 5.4→5.13, ort rc.10→rc.11, candle 0.9→0.10, hf-hub 0.4→0.5,
and tantivy 0.25→0.26 are all uninvolved. Bisect ruled out.

## Recommendation

1. Open follow-up tasks for F1, F2, F3, F4 (four tasks, each with the
   evidence above + the `[[bin]]` / route / handler references).
2. Fix F1+F2+F3 first — those block probe 2.1 onward.
3. Investigate F4 while running probe 2.2 (the real-model probe will
   naturally exercise the embedding path).
4. Resume this verification doc at 2.2 after fixes land.

`scripts/probe_snapshot.py` is kept in-tree for the resumed run
(takes `seed|verify` argument).
