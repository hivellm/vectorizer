Fix all clippy warnings and errors

- Remove unused imports (serde_json::json, Collection, Vector, create_test_collection, insert_test_vectors, Payload, Duration, sleep)
- Fix field assignment outside initializer: convert all `let mut config = CollectionConfig::default(); config.field = value` to struct literal syntax
- Fix format string warnings: use inline format strings (format!("vec_{i}") instead of format!("vec_{}", i))
- Fix unused variable warnings: prefix unused variables with underscore or remove let-binding
- Add #[allow(dead_code)] to helper functions that are used in tests but not directly referenced

All clippy checks now pass with -D warnings flag