Add automatic system service installation to install scripts

- Linux/macOS: Installs systemd service that auto-starts on boot
  - Creates /var/lib/vectorizer data directory
  - Creates vectorizer system user
  - Configures service with security hardening
  - Enables and starts service automatically
  
- Windows: Installs Windows Service that auto-starts on boot
  - Creates ProgramData\Vectorizer data directory
  - Configures service with auto-restart on failure
  - Handles Administrator privilege requirements
  
- CLI remains available in PATH for terminal usage
- Updated README with service management commands
- Updated config/vectorizer.service with correct paths

The Vectorizer server now runs as a system service that:
- Starts automatically on system boot
- Restarts automatically on failure
- Runs in background
- CLI tool remains available for manual operations