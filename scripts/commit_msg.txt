Make sparse field optional in Vector struct

- Add #[serde(default)] to sparse and payload fields for JSON deserialization
- Implement Default trait for Vector to enable ..Default::default() syntax
- Update all tests to use ..Default::default() instead of explicit sparse: None
- Simplify Vector creation by making sparse field truly optional

This change makes it easier to create Vector instances without explicitly
specifying optional fields, improving code readability and maintainability.