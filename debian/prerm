#!/bin/bash
set -e

# Stop and disable service before removal
if systemctl is-active --quiet vectorizer; then
    systemctl stop vectorizer
fi

if systemctl is-enabled --quiet vectorizer 2>/dev/null; then
    systemctl disable vectorizer
fi

#DEBHELPER#

